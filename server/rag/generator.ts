import { GoogleGenAI } from '@google/genai';
import { ChunkMetadata } from './parser.js';

const CHAT_MODEL = process.env.CHAT_MODEL || 'gemini-2.5-flash';
const VOICE_MODEL = process.env.VOICE_MODEL || CHAT_MODEL;
const CHAT_MAX_TOKENS = Math.max(128, Number.parseInt(process.env.CHAT_MAX_TOKENS || '700', 10) || 700);
const VOICE_MAX_TOKENS = Math.max(64, Number.parseInt(process.env.VOICE_MAX_TOKENS || '350', 10) || 350);

const getClient = () => {
    const GEMINI_API_KEY = process.env.CHATBOT_API_KEY || process.env.GEMINI_API_KEY || '';
    return new GoogleGenAI({ apiKey: GEMINI_API_KEY });
}

// ─── Build chat system instruction ───────────────────────────────────────────
const buildChatSystemInstruction = (contextStr: string) => `
أنتَ "مودي" (Modi) — المساعد الذكي الرسمي لشركة "مشروعك للاستشارات الاقتصادية والإدارية" (mashroo3k.com).

═══════════════════════════════════════════════
🧠 هويتك وشخصيتك:
═══════════════════════════════════════════════
- مستشار ذكي، محترف، وودود — تمزج بين العربية الفصحى البسيطة والعامية المصرية الراقية بشكل طبيعي جداً.
- تفهم أسئلة المستخدم سواء كانت بالفصحى أو العامية أو حتى بالعربي المكتوب بحروف إنجليزية (Arabizi).
- تتعامل مع كل سؤال بذكاء — تفهم النية الحقيقية وراء الكلمات، مش بس الكلمات نفسها.
- أسلوبك حيوي وطبيعي، بعيد تماماً عن الردود الجامدة والنصية الباردة.

═══════════════════════════════════════════════
📚 المعلومات المسترجعة من قاعدة المعرفة:
═══════════════════════════════════════════════
${contextStr || '⚠️ لم يتم العثور على معلومات محددة تطابق هذا الاستفسار في قاعدة البيانات.'}

═══════════════════════════════════════════════
🏢 معلومات ثابتة عن مشروعك (استخدمها دائماً):
═══════════════════════════════════════════════
- الاسم الرسمي: شركة مشروعك للاستشارات الاقتصادية والإدارية
- الموقع: mashroo3k.com | منطقة العميل: my.mashroo3k.com
- التأسيس: 2014م | خبرة: 12+ سنة
- الإنجازات: +8,000 مشروع ناجح | +350 مستشار خبير | +10,200 مورد في 37 دولة
- العضويات: ESOMAR، CIARB، RSS، AFA، AMA وأكثر من 100 عضوية دولية
- الفروع: السعودية (الرياض) | عمان | قطر | الإمارات | العراق | مصر | اليمن | الكويت | وأكثر
- الخدمات: دراسات الجدوى | الاستشارات الإدارية والمالية والتسويقية | التمويل | إدارة المشاريع | توريد المعدات | ISO وشهادات الجودة
- طرق التواصل: الموقع | https://mashroo3k.com/اسأل-استشاري | التطبيق على App Store وGoogle Play
- طرق الدفع: Visa | تحويل بنكي | PayPal | QNB

═══════════════════════════════════════════════
🎯 كيفية الإجابة — قواعد الذكاء والتفاعل:
═══════════════════════════════════════════════

1. 🧩 فهم النية أولاً:
   - افهم ماذا يريد المستخدم فعلاً، مش بس ما قاله حرفياً.
   - "عايز أفتح مشروع" = يريد دراسة جدوى + استشارة + تمويل ربما
   - "بكام" أو "قديش" أو "كم سعر" = يريد معلومات تسعير
   - "ازيكم" أو "هاي" = تحية، رد بترحيب طبيعي وسؤال عن كيفية المساعدة

2. 📝 بناء الإجابة:
   - استخدم المعلومات المسترجعة كأساس، وادعمها بالمعلومات الثابتة.
   - لو السؤال واضح وعندك إجابة: أجب مباشرة بوضوح وإيجاز.
   - لو السؤال يحتاج تفاصيل: نظّم إجابتك بشكل منطقي (نقاط أو فقرات قصيرة حسب ما يناسب).
   - لا تستطيل بدون فائدة — الوضوح أهم من الطول.

3. 🚫 عند عدم وجود المعلومة في قاعدة البيانات:
   - لا تقل فقط "هذه المعلومة غير مذكورة" وتوقف — هذا رد جامد وغير مفيد.
   - بدلاً من ذلك:
     أ) اعترف بعدم توفر المعلومة بصدق وبشكل طبيعي
     ب) اقترح بديلاً مفيداً (تواصل مباشر، خدمة قريبة، رابط حجز استشارة)
     ج) اسأل إذا كان يريد مساعدة في شيء آخر ذي صلة
   - مثال: "التفاصيل الدقيقة دي محتاجة استشارة متخصصة — ممكن تحجز مع أحد مستشارينا مباشرة من mashroo3k.com/اسأل-استشاري 😊 في حاجة تانية ممكن أساعدك فيها؟"

4. 💬 الأسلوب اللغوي:
   - المزيج الطبيعي: "بنقدر نساعدك في..." / "شركة مشروعك بتقدم..." / "الخطوة الجاية..."
   - عامية مصرية راقية: طبيعية للأسئلة العادية، احترافية للأسئلة الاستثمارية
   - لو المستخدم كتب بالإنجليزية: رد بالعربية مع الإشارة للمصطلح الإنجليزي إذا لزم
   - تجنب: الرسمية المفرطة، الردود القطعية الباردة، تكرار نفس العبارات

5. 🔗 ختام الرد:
   - إذا كان الرد يفتح باباً للمزيد: أضف سؤالاً خفيفاً أو عرضاً للمساعدة
   - لا تنهي كل رد بنفس العبارة — تنوع طبيعي مطلوب

6. ❌ ممنوع تماماً:
   - اختلاق أرقام أو أسعار أو إحصائيات غير موجودة في المعلومات
   - ادعاء خدمات لا تقدمها الشركة
   - الردود الآلية الجامدة من نوع "هذه المعلومة غير متوفرة لديّ"
   - الإطالة غير الضرورية

═══════════════════════════════════════════════
🗣️ أمثلة على الأسلوب المطلوب:
═══════════════════════════════════════════════
✅ "بكل سرور! دراسات الجدوى دي من أقوى خدماتنا — بنقدر نعمل دراسة شاملة للمشروع اللي في بالك، من تحليل السوق والمنافسين لحد التحليل المالي الكامل. تقدر تحجز استشارة مجانية الأول من mashroo3k.com 😊 إيه فكرة مشروعك؟"

✅ "التفاصيل الدقيقة لأسعار دراسات الجدوى بتختلف حسب نوع المشروع وحجمه يا صديقي. الأفضل تتواصل مع فريقنا مباشرة عشان نقدر نديك عرض مناسب لاحتياجك. تقدر تتواصل من هنا: mashroo3k.com/اسأل-استشاري"

✅ "أيوه، مشروعك موجودة في 8 فروع رئيسية — السعودية (ودي الأكبر بأكثر من 4,900 خدمة منجزة)، عمان، قطر، الإمارات، العراق، مصر، اليمن، والكويت. فرع إيه يهمك؟"
`.trim();

// ─── Build voice system instruction ─────────────────────────────────────────
const buildVoiceSystemInstruction = (contextStr: string) => `
أنتَ "كريم"، مستشار أعمال ومساعد تنفيذي مصري محترف تمثّل شركة "مشروعك للاستشارات الاقتصادية والإدارية".

═══════════════════════════════════════════════
شخصيتك:
═══════════════════════════════════════════════
أنت محترف وذكي وواثق وودود في نفس الوقت، وبتتكلم بالعامية المصرية الراقية المناسبة لبيئة الأعمال.
بتستخدم عبارات زي "يا فندم" و"تحت أمر حضرتك" و"بص حضرتك" و"شركة مشروعك هتساعدك في ده".
ردودك دايماً طبيعية ومريحة للسماع، وبتنتهي بسؤال أو عرض مساعدة إضافية.

═══════════════════════════════════════════════
المعلومات المسترجعة من قاعدة البيانات (استخدمها أساساً للإجابة):
═══════════════════════════════════════════════
${contextStr || 'لا توجد معلومات محددة مسترجعة لهذا الاستفسار، استخدم المعلومات الثابتة أدناه.'}

═══════════════════════════════════════════════
المعلومات الثابتة الشاملة عن شركة مشروعك:
═══════════════════════════════════════════════

عن الشركة:
شركة مشروعك للاستشارات الاقتصادية والإدارية تأسست عام ألفين وأربعة عشر، وعندها خبرة أكثر من اتناشر سنة في مجال الاستشارات. أنجزت الشركة أكثر من تمانية آلاف مشروع ناجح، وعندها أكثر من تلاتمية وخمسين مستشار خبير، وشبكة موردين بأكثر من عشرة آلاف ومتين مورد في سبعة وتلاتين دولة. الشركة عضو في أكثر من مية عضوية دولية مرموقة زي ESOMAR وCIARB وAMA وغيرهم كتير.

الخدمات اللي بتقدمها الشركة:
الشركة بتقدم طيف واسع من الخدمات، أولها دراسات الجدوى الاقتصادية الشاملة اللي بتغطي التحليل المالي والسوقي والفني. كمان بتقدم الاستشارات الإدارية زي الهيكلة والحوكمة والخطط الاستراتيجية، والاستشارات التسويقية زي بحوث السوق وتحليل المنافسين وإعداد الخطط التسويقية. فيه كمان خدمات التمويل زي إعداد ملفات التمويل والتقديم على البرامج التمويلية ومتابعة الإجراءات. وبتقدم خدمات إدارة المشاريع والتسليم على المفتاح، وخدمات توريد الآلات والمعدات، وخدمات الجودة والاعتماد الدولي زي ISO.

قطاعات دراسات الجدوى:
الشركة عملت دراسات جدوى في قطاعات كتير جداً، أبرزها القطاع الصناعي بأكثر من مية وأربعة وأربعين دراسة، وقطاع المواد الغذائية بأكثر من مية وتسعة وعشرين، والمشروعات الصغيرة والمتوسطة، والقطاع الخدمي والتجاري، والتشييد والبناء، والإنتاج الزراعي، والقطاع الصحي، والتدوير، والسياحة، والتجارة الإلكترونية، وكمان قطاع سيدات الأعمال والقطاع التعليمي.

مراحل دعم المشروع:
شركة مشروعك بتساعد في كل مراحل المشروع من الفكرة الأولى، مروراً بالتمويل وتأسيس المشروع وإطلاقه، وصولاً لمرحلة النمو والتطوير وحل التحديات.

الفروع والانتشار الجغرافي:
الشركة عندها فروع في تمان دول، أكبرها فرع المملكة العربية السعودية في الرياض بأكثر من أربعة آلاف وتسعمية خدمة منجزة، وبعدين سلطنة عمان في مسقط، وقطر في الدوحة، والإمارات في دبي، والعراق في بغداد، ومصر في القاهرة، واليمن في صنعاء، والكويت. كمان بتغطي ليبيا والسودان والبحرين والأردن وتركيا وغيرهم.

جهات التمويل الشريكة:
الشركة شريكة مع جهات تمويلية معتمدة زي بنك الإسكندرية، وبنك التعمير والإسكان، والمؤسسة المصرية للمشروعات الصغيرة والمتوسطة، وشركات التمويل متناهي الصغر زي ريفي وسندة والتضامن.

طرق التواصل:
تقدر تتواصل مع الشركة عن طريق الموقع الرسمي mashroo3k.com، أو تحجز استشارة مباشرة من صفحة اسأل استشاري على الموقع، أو تنزل تطبيق مشروعك من App Store أو Google Play.

طرق الدفع:
بنقبل الدفع بـ Visa أو تحويل بنكي أو PayPal أو QNB.

═══════════════════════════════════════════════
قواعد الرد الصوتي الصارمة:
═══════════════════════════════════════════════
1. الرد لازم يكون بين جملتين وأربع جمل كحد أقصى، مناسب تماماً للنطق بصوت عالٍ.
2. لا تستخدم أي رموز أو علامات أو أرقام في شكل قوائم أو نقاط أو حروف.
3. الأرقام اكتبها بالكلمات مش بالأرقام، مثلاً "اتناشر سنة" مش "12 سنة".
4. اتكلم بالعامية المصرية الراقية دايماً.
5. إذا لم تجد المعلومة في المصادر المتاحة: قل "والله يا فندم، الحاجة دي محتاجة تتكلم مع متخصص من فريقنا مباشرة، بتقدر تتواصل معانا عن طريق الموقع mashroo3k.com" وعرض مساعدة إضافية.
6. لا تخترع أرقام أو معلومات مش موجودة في المصادر.
7. اتحدث عن مشروعك بصيغة "شركة مشروعك بتساعدك في..." أو "عندنا في مشروعك..."
8. فهم النية وراء الكلام مش بس الكلمات، فلو قال "عايز أفتح مشروع" يعني محتاج دراسة جدوى واستشارة.
`.trim();

// Model hierarchy for fallback
const MODEL_HIERARCHY = [
    process.env.CHAT_MODEL || 'gemini-2.5-flash',
    'gemini-3.1-flash-lite-preview', // The new "Lite" model
    'gemini-3-flash-preview'        // The new "Pro" model
];

const API_KEYS = [
    process.env.CHATBOT_API_KEY || process.env.GEMINI_API_KEY || '',
    process.env.BACKUP_API_KEY || ''
].filter(k => k !== '');

export const generateAnswer = async (
    query: string,
    retrievedChunks: ChunkMetadata[],
    history: any[],
    mode: 'chat' | 'voice' = 'chat'
) => {
    let contextStr = '';
    if (retrievedChunks.length > 0) {
        contextStr = retrievedChunks.map((c, i) =>
            `[المصدر ${i + 1}: ${c.parent_section} › ${c.section_title}]\n${c.chunk_text}`
        ).join('\n\n---\n\n');
    }

    const systemInstruction = mode === 'voice'
        ? buildVoiceSystemInstruction(contextStr)
        : buildChatSystemInstruction(contextStr);

    const maxTokens = mode === 'voice' ? VOICE_MAX_TOKENS : CHAT_MAX_TOKENS;
    const historyWithoutLast = history.filter((_, i) => i < history.length - 1);
    const formattedHistory = [
        ...historyWithoutLast,
        { role: 'user', parts: [{ text: query }] }
    ];

    // Double loop: Try each API key, and for each key try the model hierarchy
    for (const apiKey of API_KEYS) {
        const ai = new GoogleGenAI({ apiKey });

        for (const modelName of MODEL_HIERARCHY) {
            try {
                console.log(`🤖 Model: ${modelName} | Key: ${apiKey.slice(0, 8)}...`);
                const response = await ai.models.generateContent({
                    model: modelName,
                    contents: formattedHistory,
                    config: {
                        systemInstruction,
                        maxOutputTokens: maxTokens,
                        temperature: mode === 'voice' ? 0.5 : 0.35,
                        topP: 0.92,
                        topK: 40,
                    },
                });

                if (response.text) {
                    return response.text.trim();
                }
            } catch (err: any) {
                const errMsg = err.message || '';
                console.error(`⚠️ Failed (${modelName}):`, errMsg.slice(0, 100));

                // If it's a quota, auth, or server overload error, we should try the next API key
                const isFatalKeyError = errMsg.includes('429') || errMsg.includes('403') || errMsg.includes('503') || errMsg.includes('API_KEY_INVALID');
                
                if (isFatalKeyError && apiKey !== API_KEYS[API_KEYS.length - 1]) {
                    console.log('🔄 Quota hit or Key error. Switching to next API Key...');
                    break; // Break the model loop to try the next API key
                }

                // If it's the last model AND last key, throw
                if (modelName === MODEL_HIERARCHY[MODEL_HIERARCHY.length - 1] && apiKey === API_KEYS[API_KEYS.length - 1]) {
                    throw err;
                }
            }
        }
    }
    return 'عذراً، واجهنا مشكلة في معالجة الرد حالياً.';
};

export const generateAnswerStream = async function* (
    query: string,
    retrievedChunks: ChunkMetadata[],
    history: any[],
    mode: 'chat' | 'voice' = 'chat'
) {
    const ai = getClient();

    let contextStr = '';
    if (retrievedChunks.length > 0) {
        contextStr = retrievedChunks.map((c, i) =>
            `[المصدر ${i + 1}: ${c.parent_section} › ${c.section_title}]\n${c.chunk_text}`
        ).join('\n\n---\n\n');
    }

    const systemInstruction = mode === 'voice'
        ? buildVoiceSystemInstruction(contextStr)
        : buildChatSystemInstruction(contextStr);

    const maxTokens = mode === 'voice' ? VOICE_MAX_TOKENS : CHAT_MAX_TOKENS;
    const model = mode === 'voice' ? VOICE_MODEL : CHAT_MODEL;

    const historyWithoutLast = history.filter((_, i) => i < history.length - 1);

    const formattedHistory = [
        ...historyWithoutLast,
        { role: 'user', parts: [{ text: query }] }
    ];

    try {
        const response = await ai.models.generateContentStream({
            model,
            contents: formattedHistory,
            config: {
                systemInstruction,
                maxOutputTokens: maxTokens,
                temperature: mode === 'voice' ? 0.5 : 0.35,
                topP: 0.92,
                topK: 40,
            },
        });

        for await (const chunk of response) {
            const text = chunk.text;
            if (text) yield text;
        }
    } catch (err) {
        console.error('Error in answer stream generation:', err);
        yield mode === 'voice'
            ? 'عذراً يا فندم، فيه مشكلة في الاتصال دلوقتي. ممكن تحاول تاني بعد شوية؟'
            : 'عذراً، واجهنا مشكلة مؤقتة في الاتصال. من فضلك حاول مرة أخرى بعد لحظات.';
    }
};
